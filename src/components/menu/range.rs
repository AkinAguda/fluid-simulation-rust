use percy_dom::*;

use crate::components::*;

pub struct Range {
    title: String,
}

impl View for Range {
    fn render(&self) -> VirtualNode {
        html! {
            <div>

            </div>
        }
    }
}
